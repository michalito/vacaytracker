# ===========================================
# VacayTracker Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then start with Docker Compose:
#   docker compose up --build

# ===========================================
# Authentication (REQUIRED)
# ===========================================

# JWT secret for token signing - MUST be at least 32 characters
# Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=your-secure-secret-key-minimum-32-characters-long

# Initial admin password (change after first login!)
ADMIN_PASSWORD=admin123

# Admin account details
ADMIN_EMAIL=admin@company.com
ADMIN_NAME=Captain Admin

# ===========================================
# Email Configuration (Optional)
# ===========================================
# VacayTracker uses Resend (https://resend.com) for sending emails.
# Leave these empty to disable email notifications.
#
# To enable emails:
# 1. Create a free account at https://resend.com
# 2. Get your API key from the dashboard
# 3. Verify your domain OR use onboarding@resend.dev for testing

# Your Resend API key (starts with "re_")
RESEND_API_KEY=

# Sender email address (must be verified in Resend, or use onboarding@resend.dev)
EMAIL_FROM_ADDRESS=

# Sender display name (shown in email clients)
EMAIL_FROM_NAME=VacayTracker
